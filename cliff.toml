# git-cliff configuration
# https://git-cliff.org/docs/configuration

[changelog]
header = """# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).\n
"""
body = """
{%- macro remote_url() -%}
  https://github.com/Unarr-app/unarr-cli
{%- endmacro -%}

{% if version -%}
  ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{%- else -%}
  ## [Unreleased]
{%- endif %}

{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | striptags | trim | upper_first }}
{% for commit in commits
| filter(attribute="scope")
| sort(attribute="scope") %}
  - **{{ commit.scope }}**: {{ commit.message }}
    {%- if commit.breaking %} (**BREAKING**){% endif %}
{%- endfor -%}
{% for commit in commits %}
  {%- if not commit.scope %}
  - {{ commit.message }}
    {%- if commit.breaking %} (**BREAKING**){% endif %}
  {%- endif %}
{%- endfor %}
{% endfor %}
"""
footer = """
{%- macro remote_url() -%}
  https://github.com/Unarr-app/unarr-cli
{%- endmacro -%}

{% for release in releases -%}
  {% if release.version -%}
    {% if release.previous.version -%}
      [{{ release.version | trim_start_matches(pat="v") }}]: {{ self::remote_url() }}/compare/{{ release.previous.version }}...{{ release.version }}
    {% else -%}
      [{{ release.version | trim_start_matches(pat="v") }}]: {{ self::remote_url() }}/releases/tag/{{ release.version }}
    {% endif -%}
  {% else -%}
    {% if release.previous.version -%}
      [Unreleased]: {{ self::remote_url() }}/compare/{{ release.previous.version }}...HEAD
    {% endif -%}
  {% endif -%}
{% endfor %}
"""
trim = true

[git]
conventional_commits = true
filter_unconventional = true
split_commits = false
# Git merge commits ("Merge branch ...", "Merge pull request ...", "Merge
# feat/x into main") and GitHub's web-UI "Create CNAME" / "Delete CNAME" are not
# conventional, so filter_unconventional already dropped them — but as PARSE
# ERRORS, which printed "69 commit(s) were skipped due to parse error(s)" on
# every release and buried any real malformed commit in that count.
# Preprocessors run before conventional parsing, so rewrite them to a type the
# first parser below skips: same changelog, no warning. The patterns are
# case-sensitive on purpose: lowercase "merge: ..." commits ARE conventional and
# render under "### Merge", so they must not be caught here.
commit_preprocessors = [
  { pattern = '^Merge ', replace = "unconventional: Merge " },
  { pattern = '^(Create|Delete) CNAME', replace = "unconventional: $1 CNAME" },
]
commit_parsers = [
  { message = "^unconventional: ", skip = true },
  { message = "^feat", group = "Added" },
  { message = "^fix", group = "Fixed" },
  { message = "^perf", group = "Performance" },
  { message = "^refactor", group = "Changed" },
  { message = "^style", group = "Changed" },
  { message = "^doc", group = "Documentation" },
  { message = "^ci", group = "CI/CD" },
  { message = "^chore\\(deps\\)", skip = true },
  { message = "^chore", group = "Other" },
  { message = "^test", skip = true },
]
protect_breaking_commits = false
filter_commits = false
tag_pattern = "v[0-9].*"
sort_commits = "newest"
